This directory preserves selected Lab 2 artifacts that are still useful for historical reference.

`last_run/` contains the retained final Lab 2 training notebooks, checkpoints, and run outputs that used to live at the repo root.

Lab 2 writeups remain in `docs/` because they are still referenced by the Lab 3 rubric and guide current Lab 3 notebook decisions.

Older generated calibration exports from active Lab 3 `runs/` folders are archived under `tmp/calibration_archive/` and may be regenerated by future runs when needed.
